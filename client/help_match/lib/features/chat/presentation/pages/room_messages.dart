import 'package:flutter/material.dart';

class RoomMessagesPage extends StatelessWidget {
  const RoomMessagesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
